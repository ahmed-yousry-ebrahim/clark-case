== README
Clark Case application

A simple rails api application including a user authentication, authorization, posts and comments

The api is documented using rails api pie and you can access it locally http://localhost:3000/apipie 